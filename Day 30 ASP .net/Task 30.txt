1.Create a Gridview and get data from the DB and display it in the GridView?

<%@ Page Language="C#" AutoEventWireup="true"
    CodeBehind="Student.aspx.cs"
    Inherits="StudentGridView.Student" %>

<!DOCTYPE html>

<html>
<head runat="server">
    <title>Student Details</title>
</head>
<body>

<form id="form1" runat="server">

    <div>
        <h2>Student Information</h2>

        <asp:GridView
            ID="GridView1"
            runat="server"
            AutoGenerateColumns="true"
            BorderWidth="1"
            CellPadding="5">
        </asp:GridView>

    </div>

</form>

</body>
</html>

----------------------

using System;
using System.Data;
using System.Data.SqlClient;

namespace StudentGridView
{
    public partial class Student : System.Web.UI.Page
    {
        string conStr =@"Data Source=.\SQLEXPRESS;Initial Catalog=StudentDB;Integrated Security=True;TrustServerCertificate=True";
        protected void Page_Load(object sender, EventArgs e)
        {
            if (!IsPostBack)
            {
                GetStudentData();
            }
        }

        private void GetStudentData()
        {
            SqlConnection con = new SqlConnection(conStr);

            string query = "SELECT * FROM Student";

            SqlDataAdapter da = new SqlDataAdapter(query, con);

            DataTable dt = new DataTable();

            da.Fill(dt);

            GridView1.DataSource = dt;
            GridView1.DataBind();
        }
    }
}